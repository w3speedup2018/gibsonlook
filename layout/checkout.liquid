<!DOCTYPE html>
<html lang="{{ locale }}" dir="{{ direction }}" class="{{ checkout_html_classes }}">
  <head>
    <!-- Google Tag Manager -->
<script>(function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':
new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],
j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';j.async=true;j.src=
'https://www.googletagmanager.com/gtm.js?id='+i+dl;f.parentNode.insertBefore(j,f);
})(window,document,'script','dataLayer','GTM-NHSZ5QN');</script>
<!-- End Google Tag Manager -->
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, height=device-height, minimum-scale=1.0, user-scalable=0">
    <meta name="referrer" content="origin">

    <title>{{ page_title }}</title>

    {{ content_for_header }}

    {{ checkout_stylesheets }}
    {{ checkout_scripts }}
  
  {% render 'shogun-head' %}
  <style>
    [data-alternative-payments] {
      display: none;
    }
  </style>
</head>
  <body>
    <!-- Google Tag Manager (noscript) -->
<noscript><iframe src="https://www.googletagmanager.com/ns.html?id=GTM-NHSZ5QN"
height="0" width="0" style="display:none;visibility:hidden"></iframe></noscript>
<!-- End Google Tag Manager (noscript) -->
    {{ skip_to_content_link }}

    <header class="banner" data-header role="banner">
      <div class="wrap">
        {{ content_for_logo }}
      </div>
    </header>

    {{ order_summary_toggle }}
    <div class="content" data-content>
      <div class="wrap">
        <div class="main">
          <header class="main__header" role="banner">
            {{ content_for_logo }}
            {{ breadcrumb }}
            {{ alternative_payment_methods }}
          </header>
          <main class="main__content" role="main">
            {{ content_for_layout }}
          </main>
          <footer class="main__footer" role="contentinfo">
            {{ content_for_footer }}
          </footer>
        </div>
        <aside class="sidebar" role="complementary">
          <div class="sidebar__header">
            {{ content_for_logo }}
          </div>
          <div class="sidebar__content">
            {{ content_for_order_summary }}
          </div>
        </aside>
      </div>
    </div>

    {{ tracking_code }}

<!-- Loyalty Dev -->
<script src="https://cdn-widgetsrepository.yotpo.com/v1/loader/dYNS80QulebcAnLfp2ExgA" async></script>
<div class="yotpo-widget-instance" data-yotpo-instance-id="211622"></div>
<!-- End Loyalty Dev -->


    
    <script src="//ajax.googleapis.com/ajax/libs/jquery/2.2.4/jquery.min.js" type="text/javascript"></script>
     
    
    <script>
      
                    
      
       $(document).ready(function ()
       {  
         $("span.spanMessage").text("");
          if($(".reduction-code__text").length>0){
              		var discountCode = $("span.tag__text span.reduction-code span.reduction-code__text").text().trim();//$("span.reduction-code").text().trim();
            
                    //if(discountCode.toUpperCase()=='HOLIDAY20' ){
                     
                     if (jQuery(".spanMessage").length==0){
                       
                        jQuery(".order-summary__section.order-summary__section--total-lines").after("<span style='font-weight: 500; color: #D88B78; border: 2px solid #D88B78; padding: 10px 20px;' class='spanMessage'>By using code "+discountCode.toUpperCase()+", you are subscribing to Gibsonlook emails</span>");
                      // window.location.reload();
                     //}
                   }
              }
         
       });
      
       //$(document).on("click", "button[data-trekkie-id='apply_discount_button']", function ()
      $(".field__input-btn").on('click', function(event)
			{  
         
                       
         debugger;

                //var discountCode = $('input[data-trekkie-id="reduction_code_field"]').val().trim();
        		if(!$('#checkout_reduction_code_mobile').val()){
        			var discountCode = $('#checkout_reduction_code').val();
                } else {
        			var discountCode = $('#checkout_reduction_code_mobile').val();
                }
                
                //if(discountCode.toUpperCase()=='HOLIDAY20' ){
                 //alert('discount applied'+ discountCode);
                     if (jQuery(".spanMessage").length==0){
                       
                        jQuery(".order-summary__section.order-summary__section--total-lines").after("<span style='font-weight: 500; color: #D88B78; border: 2px solid #D88B78; padding: 10px 20px;' class='spanMessage'>By using code "+discountCode.toUpperCase()+", you are subscribing to Gibsonlook emails</span>");
                       
                     }
                  // }
				
            });
        
          $(document).on("click", ".tag__button", function ()
	  { 
       
            $("span.spanMessage").hide();
             setTimeout(function(){
              window.location.reload();
            },2500);
      });
      
    </script>
    
  {% include 'smsbump_checkout_marketing_subscription' %}
  
  </body>
</html>
